// You are given an array of unique integers salary where salary[i] is the
// salary of the ith employee.
// Return the average salary of employees excluding the minimum and maximum
// salary. Answers within 10-5 of the actual answer will be accepted.

#include <iostream>
using namespace std;

class Solution {
 public:
   double average(vector<int>& salary) {
    // I could sort the array and pick the high and low, or I could do a comparison search
     // Comparison search
   }
};

int main() {
  vector<int> test = {4000, 3000, 1000, 2000};
  return 0;
}
